@page "/upload"

<div class="container">
    <div class="p-5">
        <InputFile class="fileDrop btn btn-light w-100" OnChange="FileDrop"></InputFile>
    </div>
</div>

@code {
    bool Dragging { get; set; }
    
    private void FileDrop(InputFileChangeEventArgs obj)
    {
    }
}